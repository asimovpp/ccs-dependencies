# Ubuntu with GNU compilers configuration
cc: mpicc
cxx: mpicxx
fc: mpifort

install_dir: /workspaces/ccs-libs

# System packages to install
system_packages:
  - yacc
  - flex
  - cmake
  - openmpi-bin
  - libopenmpi-dev

# Python packages to install
python_packages:
  - lit
  - flinter
  - fprettify
  - pyyaml
